using System;

class Program
{
    static void Main(string[] args)
    {
        // TODO: Create a Reference object and a Scripture object with your chosen text.

        // Example:
        // Reference ref = new Reference("Proverbs", 3, 5, 6);
        // Scripture scripture = new Scripture(ref, "Trust in the Lord with all thine heart...");

        // TODO: Create a loop that does the following:
        // 1. Clear the console: Console.Clear();
        // 2. Display the scripture text.
        // 3. Ask the user to press Enter to continue or type "quit".
        // 4. Break the loop if they type "quit" or if the scripture IsCompletelyHidden().
        // 5. Otherwise, tell the scripture to HideRandomWords(3);
    }
}